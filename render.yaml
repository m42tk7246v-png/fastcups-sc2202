services:
  - type: web
    name: fastcups-sc2202
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker -w 1 --bind 0.0.0.0:$PORT fastcups:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
